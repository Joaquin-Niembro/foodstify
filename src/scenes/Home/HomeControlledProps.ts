import {HomeStyles, Colors} from '@src/utils/styles';

const logoutIconControlProps = {
  library: 'Entypo',
  name: 'log-out',
  size: 30,
  color: Colors.light,
  styling: {},
};

export {logoutIconControlProps};
